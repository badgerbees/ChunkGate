name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: chunkgate
          POSTGRES_PASSWORD: chunkgate
          POSTGRES_DB: chunkgate
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U chunkgate -d chunkgate"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.11"
          cache: true
      - name: Start MinIO
        run: |
          docker run -d --name chunkgate-minio \
            -p 9000:9000 \
            -e MINIO_ROOT_USER=minioadmin \
            -e MINIO_ROOT_PASSWORD=minioadmin \
            quay.io/minio/minio:latest server /data
          for i in $(seq 1 60); do
            if curl -fsS http://localhost:9000/minio/health/ready; then
              exit 0
            fi
            sleep 1
          done
          docker logs chunkgate-minio
          exit 1
      - name: Start Azurite
        run: |
          docker run -d --name chunkgate-azurite \
            -p 10000:10000 \
            mcr.microsoft.com/azure-storage/azurite:latest \
            azurite-blob --blobHost 0.0.0.0 --location /data --skipApiVersionCheck
          for i in $(seq 1 60); do
            if curl -sS http://localhost:10000/devstoreaccount1 >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          docker logs chunkgate-azurite
          exit 1
      - name: Start fake-gcs-server
        run: |
          docker run -d --name chunkgate-fake-gcs \
            -p 4443:4443 \
            fsouza/fake-gcs-server:latest \
            -scheme http -host 0.0.0.0 -port 4443
          for i in $(seq 1 60); do
            if curl -fsS http://localhost:4443/storage/v1/b >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          docker logs chunkgate-fake-gcs
          exit 1
      - run: go test ./...
        env:
          CHUNKGATE_S3_TEST_ENDPOINT: http://localhost:9000
          CHUNKGATE_S3_TEST_BUCKET: chunkgate-test
          CHUNKGATE_S3_TEST_ACCESS_KEY_ID: minioadmin
          CHUNKGATE_S3_TEST_SECRET_ACCESS_KEY: minioadmin
          CHUNKGATE_AZURE_TEST_ENDPOINT: http://localhost:10000/devstoreaccount1
          CHUNKGATE_AZURE_TEST_ACCOUNT_NAME: devstoreaccount1
          CHUNKGATE_AZURE_TEST_ACCOUNT_KEY: Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==
          CHUNKGATE_AZURE_TEST_CONTAINER: chunkgate-test
          CHUNKGATE_GCS_TEST_ENDPOINT: http://localhost:4443/storage/v1/
          CHUNKGATE_GCS_TEST_BUCKET: chunkgate-test
          CHUNKGATE_GCS_TEST_PROJECT_ID: chunkgate-test
          CHUNKGATE_POSTGRES_TEST_DSN: postgres://chunkgate:chunkgate@localhost:5432/chunkgate?sslmode=disable
      - run: go vet ./...
      - run: go build -o /tmp/chunkgate ./cmd/chunkgate
      - run: go build -o /tmp/chunkgate-delta ./cmd/chunkgate-delta

  security:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.11"
          cache: true
      - name: Go vulnerability scan
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          "$(go env GOPATH)/bin/govulncheck" ./...
      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          path: .
          format: cyclonedx-json
          output-file: sbom.cdx.json
      - uses: actions/upload-artifact@v4
        with:
          name: chunkgate-sbom
          path: sbom.cdx.json
      - name: Filesystem vulnerability scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          format: table
          exit-code: "1"
          severity: CRITICAL,HIGH
